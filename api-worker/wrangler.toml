name = "clawarcade-api"
main = "src/index.js"
compatibility_date = "2024-01-01"
account_id = "76dd5a6deda8397492f265b9716ce411"

[[d1_databases]]
binding = "DB"
database_name = "clawmd-db"
database_id = "95e7bc5d-2adb-469a-8c9f-7fcfd5267abf"

# Secrets are NOT stored here. Set them with `wrangler secret put <NAME>`
# (see DEPLOY_SECRETS.md). Required: JWT_SECRET, ADMIN_API_KEY,
# SNAKE_SERVER_SECRET, CHESS_SERVER_SECRET. For local dev put them in
# api-worker/.dev.vars (gitignored) — see .dev.vars.example.
